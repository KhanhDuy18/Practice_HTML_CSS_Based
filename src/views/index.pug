extends _layouts/layout

block append vars
  - title = $translator.home ? $translator.home.title : "Home";

block container
  .page-container
  include ./$components/bannerSection
  include ./$components/WWDSection
  include ./$components/SOWSection
